apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: example
  type: NodePort
  ports:
    - protocol: TCP
      nodePort: 31000
      port: 80
      targetPort: 3000
  type: ClusterIP